import React from 'react';

import AccountLayout from './components/account-layout';

const AccountUI = () => {
  return (
    <React.Fragment>
      <AccountLayout />
    </React.Fragment>
  );
};

export default AccountUI;
